name: 🚀 Heneria Nexus CI/CD Pipeline

on:
  push:
    branches: [ main, develop, feature/* ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:
    inputs:
      deploy:
        description: 'Deploy to server after successful build'
        required: false
        default: false
        type: boolean

env:
  MAVEN_OPTS: -Dhttp.keepAlive=false -Dmaven.wagon.http.pool=false -Dmaven.wagon.httpconnectionManager.ttlSeconds=120

jobs:
  # Job 1: Analyse statique et validation
  validate-project:
    name: 📊 Validation & Analyse
    runs-on: ubuntu-latest
    
    outputs:
      version: ${{ steps.get-version.outputs.version }}
      has-changes: ${{ steps.changes.outputs.has-changes }}
    
    steps:
    - name: 🔄 Checkout du code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: ☕ Configuration Java 21
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '21'
        cache: maven

    - name: 🔍 Détection des changements
      id: changes
      run: |
        if git diff --name-only ${{ github.event.before }} ${{ github.sha }} | grep -E '\.(java|xml|yml|yaml|properties)$'; then
          echo "has-changes=true" >> $GITHUB_OUTPUT
          echo "📝 Changements détectés dans le code source"
        else
          echo "has-changes=false" >> $GITHUB_OUTPUT
          echo "ℹ️ Aucun changement dans le code source"
        fi

    - name: 🏷️ Extraction de la version
      id: get-version
      run: |
        VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        echo "📋 Version du projet: $VERSION"

    - name: 🔍 Validation de la structure du projet
      run: |
        echo "🏗️ Validation de la structure..."
        
        # Vérifier les fichiers essentiels
        files_to_check=(
          "pom.xml"
          "src/main/java/fr/heneria/nexus/Nexus.java"
          "src/main/resources/plugin.yml"
          "src/main/resources/db/migration"
        )
        
        for file in "${files_to_check[@]}"; do
          if [[ -e "$file" ]]; then
            echo "✅ $file"
          else
            echo "❌ $file manquant"
            echo "::error::Fichier manquant: $file"
            exit 1
          fi
        done
    - name: 🔧 Validation Maven
      run: |
        echo "🔍 Validation du projet Maven..."
        mvn validate -B -q
        
        echo ""
        echo "📊 Informations du projet:"
        mvn help:evaluate -Dexpression=project.groupId -q -DforceStdout | sed 's/^/  Group ID: /'
        mvn help:evaluate -Dexpression=project.artifactId -q -DforceStdout | sed 's/^/  Artifact ID: /'
        mvn help:evaluate -Dexpression=project.version -q -DforceStdout | sed 's/^/  Version: /'

    - name: 🧹 Analyse des dépendances
      run: |
        echo "🔍 Analyse des dépendances..."
        
        # Créer le rapport de dépendances
        mvn dependency:analyze -B -q > dependency-analysis.txt 2>&1 || true
        
        echo "📊 Dépendances principales:"
        mvn dependency:list -B | grep -E "(mariadb|hikari|triumph|flyway|paper)" | sed 's/^/  /'
        
        echo ""
        echo "⚠️ Alertes potentielles:"
        if grep -E "(WARNING|ERROR)" dependency-analysis.txt; then
          echo "Des alertes ont été détectées dans l'analyse des dépendances"
        else
          echo "✅ Aucune alerte détectée"
        fi

  # Job 2: Build et packaging
  build:
    name: 🔨 Build & Package
    runs-on: ubuntu-latest
    needs: validate-project
    if: needs.validate-project.outputs.has-changes == 'true'

    services:
      mariadb:
        image: mariadb:10.11
        env:
          MYSQL_ROOT_PASSWORD: testpassword123
          MYSQL_DATABASE: nexus_test
          MYSQL_USER: nexus_test
          MYSQL_PASSWORD: nexus_test_password
        ports:
          - 3306:3306
        options: >-
          --health-cmd="healthcheck.sh --connect --innodb_initialized"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    outputs:
      jar-name: ${{ steps.build-info.outputs.jar-name }}
      jar-size: ${{ steps.build-info.outputs.jar-size }}

    steps:
    - name: 🔄 Checkout du code
      uses: actions/checkout@v4

    - name: ☕ Configuration Java 21
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '21'
        cache: maven

    - name: 🗃️ Vérification de la base de données
      run: |
        echo "🔍 Test de la base de données de test..."
        
        # Attendre que MariaDB soit prêt
        for i in {1..30}; do
          if mysql -h127.0.0.1 -P3306 -uroot -ptestpassword123 -e "SELECT 1;" &>/dev/null; then
            echo "✅ MariaDB est prêt"
            break
          fi
          echo "⏳ Attente de MariaDB... ($i/30)"
          sleep 2
        done
        
        # Vérifier la base de test
        mysql -h127.0.0.1 -P3306 -uroot -ptestpassword123 -e "SHOW DATABASES;" | grep nexus_test
        echo "✅ Base de données nexus_test disponible"

    - name: 🔧 Compilation
      run: |
        echo "🔨 Compilation du projet..."
        mvn clean compile -B -q
        
        if [[ $? -eq 0 ]]; then
          echo "✅ Compilation réussie"
          echo "📁 Classes compilées:"
          find target/classes -name "*.class" | head -10 | sed 's/^/  /'
        else
          echo "❌ Échec de la compilation"
          exit 1
        fi

    - name: 📦 Packaging
      id: package
      run: |
        echo "📦 Création du package..."
        mvn package -B -DskipTests -q
        
        if [[ $? -eq 0 ]]; then
          echo "✅ Packaging réussi"
        else
          echo "❌ Échec du packaging"
          exit 1
        fi

    - name: 🔍 Analyse du JAR
      id: build-info
      run: |
        echo "🔍 Analyse du JAR créé..."

        JAR_FILE=$(find target -name "*.jar" -not -name "*-sources.jar" -not -name "original-*" | head -1)
        JAR_NAME=$(basename "$JAR_FILE")
        JAR_SIZE=$(ls -lh "$JAR_FILE" | awk '{print $5}')

        echo "jar-name=$JAR_NAME" >> $GITHUB_OUTPUT
        echo "jar-size=$JAR_SIZE" >> $GITHUB_OUTPUT

        echo "📊 Informations du JAR:"
        echo "  📁 Nom: $JAR_NAME"
        echo "  📏 Taille: $JAR_SIZE"

        # Vérifier la taille minimale attendue (doit être > 5MB)
        JAR_SIZE_BYTES=$(stat -c%s "$JAR_FILE")
        MIN_SIZE_BYTES=5242880  # 5MB
        if [[ $JAR_SIZE_BYTES -lt $MIN_SIZE_BYTES ]]; then
            echo "⚠️ ATTENTION: JAR trop petit ($JAR_SIZE), les dépendances sont probablement manquantes"
            echo "::warning::JAR size is too small, dependencies might be missing"
        fi

        echo ""
        echo "🧪 Vérification du contenu:"

        # Vérifier les fichiers critiques avec une approche plus robuste
        critical_files=(
          "plugin.yml"
          "fr/heneria/nexus/Nexus.class"
          "org/mariadb/jdbc/Driver.class"
          "fr/heneria/nexus/libs/hikari/HikariDataSource.class"
          "fr/heneria/nexus/libs/flywaydb/core/Flyway.class"
        )

        for file in "${critical_files[@]}"; do
          if jar tf "$JAR_FILE" | grep -q "$file"; then
            echo "  ✅ $file"
          else
            echo "  ❌ $file manquant"
            echo "::error::Fichier critique manquant dans le JAR: $file"
            exit 1
          fi
        done

        # Vérification spéciale pour Triumph GUI (recherche dynamique)
        echo ""
        echo "🔍 Vérification de Triumph GUI:"
        GUI_CLASSES=$(jar tf "$JAR_FILE" | grep "fr/heneria/nexus/libs/gui/" | head -3)
        if [ -z "$GUI_CLASSES" ]; then
          echo "  ❌ Aucune classe Triumph GUI trouvée dans fr/heneria/nexus/libs/gui/"
          echo "::error::Triumph GUI non relocalisé correctement"
          exit 1
        else
          echo "  ✅ Classes Triumph GUI détectées:"
          echo "$GUI_CLASSES" | sed 's/^/    /'

          if jar tf "$JAR_FILE" | grep -q "fr/heneria/nexus/libs/gui.*Gui\\.class"; then
            echo "  ✅ Classe Gui principale trouvée"
          else
            echo "  ⚠️  Aucune classe Gui principale détectée, mais d'autres classes GUI sont présentes"
          fi
        fi

        echo ""
        echo "📈 Analyse détaillée des dépendances relocalisées:"

        count_and_display() {
          local pattern="$1"
          local name="$2"
          local count=$(jar tf "$JAR_FILE" | grep -c "$pattern" || echo "0")
          echo "  $name: $count classes"
          if [ "$count" -gt 0 ]; then
            echo "    $(jar tf "$JAR_FILE" | grep "$pattern" | head -2 | sed 's/^/    ├─ /')"
            if [ "$count" -gt 2 ]; then
              echo "    └─ ... et $((count-2)) autres"
            fi
          fi
        }

        count_and_display "org/mariadb/" "MariaDB JDBC"
        count_and_display "fr/heneria/nexus/libs/hikari/" "HikariCP (relocalisé)"
        count_and_display "fr/heneria/nexus/libs/flywaydb/" "Flyway (relocalisé)"
        count_and_display "fr/heneria/nexus/libs/gui/" "Triumph GUI (relocalisé)"

        echo ""
        echo "🔒 Vérification des fuites de relocalisation:"
        LEAKED_CLASSES=""
        for pattern in "com/zaxxer/hikari" "org/flywaydb" "dev/triumphteam/gui"; do
          if jar tf "$JAR_FILE" | grep -q "$pattern"; then
            LEAKED_CLASSES="$LEAKED_CLASSES $pattern"
          fi
        done

        if [ -n "$LEAKED_CLASSES" ]; then
          echo "  ⚠️  Classes non-relocalisées détectées: $LEAKED_CLASSES"
          echo "  Cela peut causer des conflits de versions avec d'autres plugins"
        else
          echo "  ✅ Toutes les dépendances sont correctement relocalisées"
        fi

    - name: 📤 Upload du JAR
      uses: actions/upload-artifact@v4
      with:
        name: nexus-plugin-${{ needs.validate-project.outputs.version }}
        path: target/*.jar
        retention-days: 30

  # Job 3: Tests d'intégration complets
  integration-tests:
    name: 🧪 Tests d'Intégration
    runs-on: ubuntu-latest
    needs: [validate-project, build]
    if: needs.validate-project.outputs.has-changes == 'true'

    services:
      mariadb:
        image: mariadb:10.11
        env:
          MYSQL_ROOT_PASSWORD: test_root_password
          MYSQL_DATABASE: nexus_integration_test
          MYSQL_USER: nexus_user
          MYSQL_PASSWORD: nexus_password
        ports:
          - 3306:3306
        options: >-
          --health-cmd="healthcheck.sh --connect --innodb_initialized"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
    - name: 🔄 Checkout du code
      uses: actions/checkout@v4

    - name: ☕ Configuration Java 21
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '21'
        cache: maven

    - name: 📥 Téléchargement du JAR
      uses: actions/download-artifact@v4
      with:
        name: nexus-plugin-${{ needs.validate-project.outputs.version }}
        path: target/

    - name: 🚀 Test de chargement du plugin (Smoke Test)
      run: |
        echo "🚀 Test de chargement du plugin (Smoke Test):"

        JAR_FILE=$(find target -name "*.jar" -not -name "*-sources.jar" -not -name "original-*" | head -1)
        if [ ! -f "$JAR_FILE" ]; then
            echo "❌ Aucun JAR trouvé dans target/"
            exit 1
        fi

        echo "📁 JAR trouvé: $JAR_FILE ($(ls -lh "$JAR_FILE" | awk '{print $5}'))"

        if ! javac PluginLoadTest.java; then
            echo "❌ Échec de compilation du smoke test"
            exit 1
        fi

        echo "🔧 Configuration de l'environnement de test..."
        cat > simplelogger.properties <<'EOF'
org.slf4j.simpleLogger.defaultLogLevel=error
org.slf4j.simpleLogger.showDateTime=false
org.slf4j.simpleLogger.showThreadName=false
org.slf4j.simpleLogger.showLogName=false
EOF

        if ! timeout 60s java -Dorg.slf4j.simpleLogger.defaultLogLevel=error \
             -Dorg.slf4j.simpleLogger.showDateTime=false \
             -Dorg.slf4j.simpleLogger.showThreadName=false \
             -Dorg.slf4j.simpleLogger.showLogName=false \
             -cp "$JAR_FILE:." PluginLoadTest "$JAR_FILE"; then
            echo "❌ Échec ou timeout du smoke test"
            exit 1
        fi

        echo "✅ Smoke Test terminé avec succès"

    - name: 🔍 Validation Supplémentaire de la Structure JAR
      run: |
        echo "🔍 Validation approfondie de la structure du JAR..."

        JAR_FILE=$(find target -name "*.jar" -not -name "*-sources.jar" -not -name "original-*" | head -1)

        echo "📦 Analyse de la structure des packages:"

        if ! javac JarAnalysis.java; then
            echo "❌ Échec de compilation de JarAnalysis"
            exit 1
        fi

        java JarAnalysis "$JAR_FILE"

    - name: 🎯 Tests de chargement des classes
      run: |
        echo "🧪 Test de chargement des classes critiques..."

        JAR_FILE=$(find target -name "*.jar" -not -name "*-sources.jar" | head -1)

        # Attendre que MariaDB soit complètement prêt
        echo "⏳ Attente de MariaDB (timeout 60s)..."
        for i in {1..60}; do
          if mysql -h127.0.0.1 -P3306 -unexus_user -pnexus_password nexus_integration_test -e "SELECT 1;" &>/dev/null; then
            echo "✅ MariaDB est prêt après ${i}s"
            break
          fi
          if [ $i -eq 60 ]; then
            echo "❌ Timeout MariaDB après 60s"
            # Diagnostics
            echo "🔍 Diagnostics MariaDB:"
            docker ps | grep mariadb || echo "Aucun container MariaDB trouvé"
            mysql -h127.0.0.1 -P3306 -uroot -ptest_root_password -e "SHOW DATABASES;" 2>&1 || echo "Connexion root échouée"
            netstat -tlnp | grep 3306 || echo "Port 3306 non ouvert"
            exit 1
          fi
          sleep 1
        done

        # Test avancé de connexion avec toutes les configurations possibles
        cat > DatabaseAdvancedTest.java << 'EOF'
        import java.sql.Connection;
        import java.sql.DriverManager;
        import java.sql.Statement;
        import java.sql.ResultSet;
        import java.sql.PreparedStatement;
        import java.util.Properties;

        public class DatabaseAdvancedTest {
            public static void main(String[] args) {
                System.out.println("🔍 Tests avancés de base de données...");

                try {
                    // Test 1: Chargement des drivers
                    System.out.println("📦 Test de chargement des drivers JDBC...");
                    Class.forName("org.mariadb.jdbc.Driver");
                    System.out.println("  ✅ Driver MariaDB chargé");

                    // Test 2: Configuration HikariCP (classes relocalisées)
                    System.out.println("🏊 Test HikariCP relocalisé...");
                    Class<?> hikariConfigClass = Class.forName("fr.heneria.nexus.libs.hikari.HikariConfig");
                    Class<?> hikariDataSourceClass = Class.forName("fr.heneria.nexus.libs.hikari.HikariDataSource");

                    Object hikariConfig = hikariConfigClass.getDeclaredConstructor().newInstance();
                    System.out.println("  ✅ HikariConfig instancié: " + hikariConfig.getClass().getName());

                    // Test 3: Connexions multiples avec différentes configurations
                    String[] urls = {
                        "jdbc:mariadb://localhost:3306/nexus_integration_test",
                        "jdbc:mariadb://127.0.0.1:3306/nexus_integration_test",
                        "jdbc:mariadb://localhost:3306/nexus_integration_test?useSSL=false",
                        "jdbc:mariadb://localhost:3306/nexus_integration_test?allowMultiQueries=true"
                    };

                    for (String url : urls) {
                        try {
                            System.out.println("🔗 Test connexion: " + url);
                            Connection conn = DriverManager.getConnection(url, "nexus_user", "nexus_password");

                            // Test basique
                            Statement stmt = conn.createStatement();
                            ResultSet rs = stmt.executeQuery("SELECT VERSION() as version, DATABASE() as db");
                            if (rs.next()) {
                                System.out.println("    ✅ Connecté - Version: " + rs.getString("version") + ", DB: " + rs.getString("db"));
                            }

                            conn.close();
                            break; // Première connexion réussie

                        } catch (Exception e) {
                            System.out.println("    ❌ Échec: " + e.getMessage());
                        }
                    }

                    // Test 4: Opérations de base de données complexes
                    System.out.println("🗃️  Test d'opérations de base de données...");
                    Connection conn = DriverManager.getConnection(
                        "jdbc:mariadb://localhost:3306/nexus_integration_test",
                        "nexus_user",
                        "nexus_password"
                    );

                    // Créer une table de test
                    Statement stmt = conn.createStatement();
                    stmt.executeUpdate("CREATE TABLE IF NOT EXISTS test_advanced (id INT PRIMARY KEY AUTO_INCREMENT, name VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP)");

                    // Test PreparedStatement
                    PreparedStatement pstmt = conn.prepareStatement("INSERT INTO test_advanced (name) VALUES (?)");
                    pstmt.setString(1, "Test Plugin Nexus");
                    int rows = pstmt.executeUpdate();
                    System.out.println("    ✅ Insertion réussie: " + rows + " ligne(s)");

                    // Test transaction
                    conn.setAutoCommit(false);
                    pstmt.setString(1, "Test Transaction");
                    pstmt.executeUpdate();
                    conn.commit();
                    conn.setAutoCommit(true);
                    System.out.println("    ✅ Transaction réussie");

                    // Test requête complexe
                    ResultSet rs = stmt.executeQuery("SELECT COUNT(*) as count, MAX(created_at) as latest FROM test_advanced");
                    if (rs.next()) {
                        System.out.println("    ✅ Requête complexe: " + rs.getInt("count") + " enregistrements, dernier: " + rs.getTimestamp("latest"));
                    }

                    // Test 5: Flyway relocalisé
                    System.out.println("🛫 Test Flyway relocalisé...");
                    Class<?> flywayClass = Class.forName("fr.heneria.nexus.libs.flywaydb.core.Flyway");
                    System.out.println("    ✅ Flyway relocalisé chargé: " + flywayClass.getName());

                    // Nettoyage
                    stmt.executeUpdate("DROP TABLE test_advanced");
                    conn.close();

                    System.out.println("🎉 Tous les tests de base de données avancés réussis !");

                } catch (Exception e) {
                    System.err.println("❌ Erreur dans les tests de base de données: " + e.getMessage());
                    e.printStackTrace();
                    System.exit(1);
                }
            }
        }
        EOF

        # Compiler et exécuter avec un timeout
        javac -cp "$JAR_FILE" DatabaseAdvancedTest.java
        timeout 120s java -cp "$JAR_FILE:." DatabaseAdvancedTest || {
            echo "❌ Timeout ou erreur dans les tests de base de données"
            exit 1
        }

    - name: 🩺 Diagnostics Système
      run: |
        echo "🖥️  Diagnostics de l'environnement CI..."

        echo "☕ Version Java:"
        java -version
        javac -version

        echo ""
        echo "🧠 Mémoire disponible:"
        free -h || echo "free non disponible"

        echo ""
        echo "💽 Espace disque:"
        df -h || echo "df non disponible"

        echo ""
        echo "🐋 Services Docker:"
        docker ps

        echo ""
        echo "🌐 Connectivité réseau:"
        ping -c 1 localhost >/dev/null && echo "✅ localhost OK" || echo "❌ localhost KO"
        nc -z localhost 3306 && echo "✅ Port 3306 ouvert" || echo "❌ Port 3306 fermé"

        echo ""
        echo "📦 JAR généré:"
        ls -la target/*.jar 2>/dev/null || echo "Aucun JAR trouvé"

        echo ""
        echo "🔍 Variables d'environnement importantes:"
        echo "JAVA_HOME: ${JAVA_HOME:-non défini}"
        echo "MAVEN_OPTS: ${MAVEN_OPTS:-non défini}"
        echo "PATH: ${PATH}"

    - name: 🚀 Tests de Performance Basic
      run: |
        echo "⚡ Tests de performance du plugin..."

        JAR_FILE=$(find target -name "*.jar" -not -name "*-sources.jar" | head -1)

        cat > PerformanceTest.java << 'EOF'
        import java.util.concurrent.*;
        import java.sql.*;

        public class PerformanceTest {
            public static void main(String[] args) {
                System.out.println("⚡ Tests de performance basiques...");

                try {
                    // Test 1: Chargement de classes en parallèle
                    testParallelClassLoading();

                    // Test 2: Connexions simultanées à la base
                    testConcurrentDatabaseConnections();

                    // Test 3: Performance mémoire
                    testMemoryUsage();

                    System.out.println("🎉 Tests de performance réussis !");

                } catch (Exception e) {
                    System.err.println("❌ Erreur dans les tests de performance: " + e.getMessage());
                    e.printStackTrace();
                    System.exit(1);
                }
            }

            private static void testParallelClassLoading() throws Exception {
                System.out.println("🔄 Test de chargement de classes en parallèle...");

                String[] classes = {
                    "fr.heneria.nexus.libs.hikari.HikariDataSource",
                    "fr.heneria.nexus.libs.flywaydb.core.Flyway",
                    "org.mariadb.jdbc.Driver"
                };

                ExecutorService executor = Executors.newFixedThreadPool(3);
                CompletableFuture<Void>[] futures = new CompletableFuture[classes.length];

                long startTime = System.currentTimeMillis();

                for (int i = 0; i < classes.length; i++) {
                    final String className = classes[i];
                    futures[i] = CompletableFuture.runAsync(() -> {
                        try {
                            Class.forName(className);
                        } catch (Exception e) {
                            throw new RuntimeException("Échec chargement: " + className, e);
                        }
                    }, executor);
                }

                CompletableFuture.allOf(futures).get(10, TimeUnit.SECONDS);
                long endTime = System.currentTimeMillis();

                executor.shutdown();
                System.out.println("    ✅ Chargement parallèle réussi en " + (endTime - startTime) + "ms");
            }

            private static void testConcurrentDatabaseConnections() throws Exception {
                System.out.println("🗃️  Test de connexions simultanées...");

                String url = "jdbc:mariadb://localhost:3306/nexus_integration_test";
                String username = "nexus_user";
                String password = "nexus_password";

                ExecutorService executor = Executors.newFixedThreadPool(5);
                CompletableFuture<Void>[] futures = new CompletableFuture[5];

                long startTime = System.currentTimeMillis();

                for (int i = 0; i < 5; i++) {
                    final int threadId = i;
                    futures[i] = CompletableFuture.runAsync(() -> {
                        try (Connection conn = DriverManager.getConnection(url, username, password)) {
                            Statement stmt = conn.createStatement();
                            ResultSet rs = stmt.executeQuery("SELECT " + threadId + " as thread_id, NOW() as now");
                            if (rs.next()) {
                                // OK
                            }
                        } catch (Exception e) {
                            throw new RuntimeException("Connexion thread " + threadId + " échouée", e);
                        }
                    }, executor);
                }

                CompletableFuture.allOf(futures).get(30, TimeUnit.SECONDS);
                long endTime = System.currentTimeMillis();

                executor.shutdown();
                System.out.println("    ✅ Connexions simultanées réussies en " + (endTime - startTime) + "ms");
            }

            private static void testMemoryUsage() {
                System.out.println("🧠 Test d'utilisation mémoire...");

                Runtime runtime = Runtime.getRuntime();
                long startMemory = runtime.totalMemory() - runtime.freeMemory();

                // Charger plusieurs fois les classes pour tester les fuites
                for (int i = 0; i < 100; i++) {
                    try {
                        Class.forName("fr.heneria.nexus.libs.hikari.HikariDataSource");
                        Class.forName("fr.heneria.nexus.libs.flywaydb.core.Flyway");
                    } catch (Exception e) {
                        throw new RuntimeException("Erreur chargement classe iteration " + i, e);
                    }
                }

                // Forcer GC
                System.gc();
                Thread.yield();

                long endMemory = runtime.totalMemory() - runtime.freeMemory();
                long memoryIncrease = endMemory - startMemory;

                System.out.println("    📊 Mémoire utilisée: " + (memoryIncrease / 1024 / 1024) + " MB");

                if (memoryIncrease > 50 * 1024 * 1024) { // 50MB
                    throw new RuntimeException("Utilisation mémoire excessive: " + (memoryIncrease / 1024 / 1024) + " MB");
                }

                System.out.println("    ✅ Utilisation mémoire acceptable");
            }
        }
        EOF

        # Compiler et exécuter avec monitoring mémoire
        javac -cp "$JAR_FILE" PerformanceTest.java
        java -Xms256m -Xmx512m -cp "$JAR_FILE:." PerformanceTest

  # Job 4: Analyse de sécurité
  security-scan:
    name: 🛡️ Analyse de Sécurité
    runs-on: ubuntu-latest
    needs: [validate-project, build]
    if: needs.validate-project.outputs.has-changes == 'true'
    continue-on-error: true  # Ne pas faire échouer le build pour les vulnérabilités

    steps:
    - name: 🔄 Checkout du code
      uses: actions/checkout@v4

    - name: ☕ Configuration Java 21
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '21'
        cache: maven

    - name: 🛡️ Scan de sécurité des dépendances
      run: |
        echo "🔍 Analyse de sécurité des dépendances..."
        
        # Installer et exécuter OWASP Dependency Check
        mvn org.owasp:dependency-check-maven:check \
          -DfailBuildOnCVSS=8 \
          -DsuppressFailureOnError=true \
          -Dformats=ALL \
          -B -q || echo "⚠️ Des vulnérabilités ont été détectées"
        
        # Afficher un résumé si le rapport existe
        if [[ -f target/dependency-check-report.html ]]; then
          echo "📊 Rapport de sécurité généré"
          if [[ -f target/dependency-check-report.json ]]; then
            echo "🔍 Analyse du rapport JSON..."
            # Ici on pourrait parser le JSON pour extraire un résumé
          fi
        fi

    - name: 📤 Upload rapport de sécurité
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: security-report-${{ needs.validate-project.outputs.version }}
        path: target/dependency-check-report.*
        retention-days: 30

  # Job 5: Métriques et qualité
  code-quality:
    name: 📊 Qualité du Code
    runs-on: ubuntu-latest
    needs: [validate-project]
    if: needs.validate-project.outputs.has-changes == 'true'

    steps:
    - name: 🔄 Checkout du code
      uses: actions/checkout@v4

    - name: 📊 Métriques de code
      run: |
        echo "📈 Analyse des métriques du code..."
        
        # Comptage des lignes
        total_lines=$(find src -name "*.java" -exec cat {} \; | wc -l)
        java_files=$(find src -name "*.java" | wc -l)
        
        echo "📏 Total lignes de code Java: $total_lines"
        echo "📁 Nombre de fichiers Java: $java_files"
        
        if [[ $java_files -gt 0 ]]; then
          avg_lines=$((total_lines / java_files))
          echo "📊 Moyenne lignes par fichier: $avg_lines"
        fi
        
        echo ""
        echo "🏗️ Répartition par package:"
        find src -name "*.java" -exec dirname {} \; | sort | uniq -c | sort -nr | head -10
        
        echo ""
        echo "⚠️ Marqueurs de développement:"
        grep -r -n "TODO\|FIXME\|XXX" src/ || echo "✅ Aucun marqueur TODO/FIXME trouvé"

  # Job 6: Rapport final et déploiement conditionnel
  finalize:
    name: 📋 Finalisation
    runs-on: ubuntu-latest
    needs: [validate-project, build, integration-tests, security-scan, code-quality]
    if: always()

    steps:
    - name: 🔄 Checkout du code
      uses: actions/checkout@v4

    - name: 📊 Génération du rapport final détaillé
      run: |
        echo "# 🎯 Rapport CI/CD Nexus Plugin - Détaillé" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

        # Informations de build existantes...

        echo "## 🧪 Résultats des Tests" >> $GITHUB_STEP_SUMMARY
        echo "| Test | Statut | Détails |" >> $GITHUB_STEP_SUMMARY
        echo "|------|--------|---------|" >> $GITHUB_STEP_SUMMARY
        echo "| 📦 Smoke Test | ${{ job.status }} | Classes relocalisées chargées |" >> $GITHUB_STEP_SUMMARY
        echo "| 🗃️ Base de Données | ${{ job.status }} | Connexions et transactions |" >> $GITHUB_STEP_SUMMARY
        echo "| ⚡ Performance | ${{ job.status }} | Chargement parallèle et mémoire |" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

        # Métriques de qualité
        echo "## 📊 Métriques de Qualité" >> $GITHUB_STEP_SUMMARY
        JAR_FILE=$(find target -name "*.jar" -not -name "*-sources.jar" | head -1)
        if [ -f "$JAR_FILE" ]; then
          JAR_SIZE=$(ls -lh "$JAR_FILE" | awk '{print $5}')
          CLASS_COUNT=$(jar tf "$JAR_FILE" | grep "\.class$" | wc -l)
          RELOCATED_COUNT=$(jar tf "$JAR_FILE" | grep "fr/heneria/nexus/libs/" | wc -l)

          echo "- **Taille JAR**: $JAR_SIZE" >> $GITHUB_STEP_SUMMARY
          echo "- **Nombre de classes**: $CLASS_COUNT" >> $GITHUB_STEP_SUMMARY
          echo "- **Classes relocalisées**: $RELOCATED_COUNT" >> $GITHUB_STEP_SUMMARY
          echo "- **Ratio relocalisation**: $(( RELOCATED_COUNT * 100 / CLASS_COUNT ))%" >> $GITHUB_STEP_SUMMARY
        fi

        echo "" >> $GITHUB_STEP_SUMMARY
        echo "## 🔧 Recommandations" >> $GITHUB_STEP_SUMMARY
        if [[ "${{ needs.validate-project.result }}" == "success" && \
              "${{ needs.build.result }}" == "success" && \
              "${{ needs.integration-tests.result }}" == "success" ]]; then
          echo "✅ **Build Production Ready** - Tous les tests critiques passent" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🚀 Prochaines étapes suggérées:" >> $GITHUB_STEP_SUMMARY
          echo "1. Déployer sur le serveur de test" >> $GITHUB_STEP_SUMMARY
          echo "2. Tests d'intégration avec vrais joueurs" >> $GITHUB_STEP_SUMMARY
          echo "3. Monitoring des performances en production" >> $GITHUB_STEP_SUMMARY
        else
          echo "⚠️  **Corrections Nécessaires** - Voir les logs ci-dessus" >> $GITHUB_STEP_SUMMARY
        fi

        echo "" >> $GITHUB_STEP_SUMMARY
        echo "---" >> $GITHUB_STEP_SUMMARY
        echo "*Rapport généré automatiquement le $(date) par Heneria CI/CD*" >> $GITHUB_STEP_SUMMARY

    - name: 🚀 Notification de déploiement (si succès)
      if: >
        needs.validate-project.result == 'success' &&
        needs.build.result == 'success' &&
        needs.integration-tests.result == 'success' &&
        github.event.inputs.deploy == 'true'
      run: |
        echo "🚀 Le plugin est prêt pour le déploiement !"
        echo "📦 Artifact: ${{ needs.build.outputs.jar-name }}"
        echo "💾 Taille: ${{ needs.build.outputs.jar-size }}"
        echo ""
        echo "Pour déployer manuellement:"
        echo "1. Télécharger l'artifact depuis cette action"
        echo "2. L'uploader sur le serveur avec WinSCP ou le script deploy.sh"
        echo "3. Redémarrer le serveur Minecraft"

