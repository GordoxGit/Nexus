# ===================================
# 🚀 Configuration Nexus Plugin
# ===================================

# Configuration de base de données
database:
  # Informations de connexion
  host: "localhost"
  port: 3306
  database: "nexus"
  username: "nexus_user"
  password: "your_password_here"
  
  # Options de compatibilité
  compatibility:
    # Si true, utilise MySQL au lieu de MariaDB en cas d'échec
    mysql-fallback: true
    # Si true, désactive Flyway et crée les tables manuellement
    disable-flyway: false
    # Version de MariaDB/MySQL (pour compatibilité)
    server-version: "10.11"
  
  # Paramètres de connexion HikariCP
  hikari:
    maximum-pool-size: 10      # Nombre maximum de connexions
    minimum-idle: 5            # Connexions toujours disponibles
    connection-timeout: 20000    # Timeout de connexion (ms)
    idle-timeout: 600000         # Timeout d'inactivité (ms) - 10min
    max-lifetime: 1800000        # Durée de vie max d'une connexion (ms) - 30min
    leak-detection-threshold: 60000  # Détection de fuite de connexion (ms) - 1min

# Configuration du gameplay
game:
  # Configuration des arènes
  arena:
    max-players-per-arena: 24
    capture-time-seconds: 30
    respawn-time-seconds: 5
    
  # Système économique
  economy:
    kill-reward: 100
    assist-reward: 50
    victory-bonus: 200
    defeat-bonus: [100, 150]  # 1ère et 2ème défaite consécutive

# Règles de jeu dynamiques
game-rules:
  rounds-to-win: 3
  nexus-max-health: 100
  nexus-surcharges-to-destroy: 2
  energy-cell:
    capture-radius: 5.0
    capture-time-seconds: 30
  economy:
    starting-round-points: 100
    kill-reward: 100
    assist-reward: 50
    round-win-bonus: 200
  kill-attribution-time-seconds: 10

# Système de classement
ranking:
  elo:
    starting-rating: 1000
    k-factor: 32
    seasons:
      duration-months: 3
      soft-reset-percentage: 0.8

# Debug et développement
debug:
  enabled: false
  log-sql-queries: false
  performance-monitoring: true
  # Si true, affiche les stacktraces complètes
  verbose-errors: true
